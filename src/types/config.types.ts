export interface IConfig {
	environment: string;
	dbHost: string;
	dbPort: number;
	dbUser: string;
	dbPassword: string;
	dbName: string;
	cacheHost: string;
	cachePort: number;
	cacheUser: string;
	cachePassword: string;
	serverPort: number;
	weatherAPIKey: string;
}
